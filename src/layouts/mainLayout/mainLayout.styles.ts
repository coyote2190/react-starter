import { styled } from 'styled-components';

export const StyleMainLayout = styled.div`
  display: flex;
  flex-direction: column;
  min-height: 100vh;

  > main {
    padding-top: 2rem;
    padding-bottom: 2rem;
  }
`;
